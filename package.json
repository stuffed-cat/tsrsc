{
  "name": "tsrsc",
  "version": "1.0.0",
  "license": "MIT",
  "description": "this is TS-code convert to RS-code compiler of ts-code improve performance ",
  "repository": {
    "type": "git",
    "url": "git+https://example.com/stuffed-cat/tsrsc.git"
  },
  "devDependencies": {
  "@types/node": "^20.19.4"
  },
  "dependencies": {
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/main.js",
    "demo": "npm run build && node dist/main.js examples/add.ts --out examples/add.rs"
  },
  "bin": {
    "tsrsc": "dist/main.js"
  }
}
